function extractName(text) {
    // Your extraction logic for name
}

function extractDocumentNumber(text) {
    // Your extraction logic for document number
}

function extractExpirationDate(text) {
    // Your extraction logic for expiration date
}

module.exports = { extractName, extractDocumentNumber, extractExpirationDate };
